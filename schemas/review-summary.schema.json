{
  "$defs": {
    "ReviewDecision": {
      "enum": [
        "pass",
        "revise"
      ],
      "title": "ReviewDecision",
      "type": "string"
    },
    "ReviewOutcome": {
      "enum": [
        "revise",
        "watch",
        "pass"
      ],
      "title": "ReviewOutcome",
      "type": "string"
    }
  },
  "$id": "formloop/review-summary",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Concise structured output of normal design review for one revision.",
  "properties": {
    "decision": {
      "$ref": "#/$defs/ReviewDecision"
    },
    "key_findings": {
      "items": {
        "type": "string"
      },
      "title": "Key Findings",
      "type": "array"
    },
    "next_step": {
      "description": "One concise user-facing sentence describing what should happen next.",
      "minLength": 1,
      "title": "Next Step",
      "type": "string"
    },
    "outcome": {
      "$ref": "#/$defs/ReviewOutcome"
    },
    "revision_instructions": {
      "default": "",
      "description": "Concrete instructions for the CAD Designer's next attempt. Empty string when decision is pass.",
      "title": "Revision Instructions",
      "type": "string"
    },
    "schema_version": {
      "const": 2,
      "default": 2,
      "title": "Schema Version",
      "type": "integer"
    },
    "summary": {
      "description": "One concise user-facing sentence summarizing what the review found.",
      "minLength": 1,
      "title": "Summary",
      "type": "string"
    }
  },
  "required": [
    "decision",
    "outcome",
    "summary",
    "next_step"
  ],
  "title": "ReviewSummary",
  "type": "object"
}
