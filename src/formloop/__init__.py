"""Formloop agent harness.

REQ: FLH-NF-001

Orchestrates a manager agent and a small set of specialists around the
deterministic ``cad-cli`` toolchain to turn natural-language design intent
into inspectable CAD artifacts.

See REQUIREMENTS_HARNESS.md in the repo root for the canonical requirements
list. Each module carries in-code ``REQ: FLH-*`` markers that grep back to
the requirements they realize.
"""

__version__ = "0.1.0"
